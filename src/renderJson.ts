import { Board, Section, Thread } from "./types.js";

export function asBoardJson(board: Board): Board {
  return board;
}

export function asSectionJson(section: Section): Section {
  return section;
}

export function asThreadJson(thread: Thread): Thread {
  return thread;
}
